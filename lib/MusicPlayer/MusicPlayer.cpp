#include "MusicPlayer.h"
#include <Arduino.h>

namespace MusicPlayer{
    void setup(float speed) {
        // initialize pins, etc.
    }

    void update(bool player_dry) {
        // non-blocking logic
    }
}